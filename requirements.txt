opencv-python>=4.8.0
faster-whisper>=1.0.0
pyaudio>=0.2.13
pokerkit>=0.5.0
customtkinter>=5.2.0
easyocr>=1.7.0
numpy>=1.24.0
pytest>=7.0.0
# pyscard は transport="pcsc" 時のみ必要 (transport="http" では不要)
# 個別に入れる場合: pip install pyscard>=2.0.7
# pyproject.toml の extra で入れる場合: pip install -e .[pcsc]

# Worklog: v1.0 ローンチレビューの小粒修正（B1/B2/B3/B6）

## Date

2026-06-15

## Scope / Task

`docs/reviews/2026-06-15-v1.0-launch-review.md` で抽出したブロッカーのうち、小粒で高レバレッジな
ものから着手。ユーザー確定前提: **RFID は v1.0 必須（来週実機テスト）/ 音声は自動記録・訂正 UI は iPad
アプリ側 / SaaS は後ろ倒し**。

## やったこと（4 件）

### B3 — RFID PC/SC の street 自動遷移バグ（来週の実機テスト最優先）
- `rfid/reader_thread.py:_poll_reader` が `board_index` を未設定 → engine.py:294 の分岐
  （`board_index is not None`）に到達せず、PC/SC 経路で board の flop/turn/river 自動遷移が機能せず。
  HTTP 経路（`http_receiver.py`）は設定済みで顕在化していなかった。
- 修正: `http_receiver` と同じく `cfg["index"]` から `board_index` を設定。
- 回帰: `tests/test_rfid.py` に board_index 伝播 / seat に付かないこと の 2 件。

### B6 — install docs を PC/SC canonical に
- `docs/installation.md` §5 を PC/SC（PN5180+ESP32-S3 USB CCID）正式・推奨に書き換え。実 reader_name の
  採取手順（`smartcard.System.readers`）、`pcsc_readers`(list) 設定、pcsc/http のキー差を明記。
  HTTP/PN532 は補助/debug に降格（ADR-0015/0034 と整合）。
- 注: コミット済の stale `config.json` は無し（gitignore + untracked のサンドボックス artifact）。
  クリーン install では `load_config` が `config_default.json`（pcsc_readers 入り）をコピーするため問題なし。

### B2 — データバックアップ（Crit: 会計データ消失対策）
- `core/backup.py:backup_data_files()`（存在するデータ JSON を `backups/<timestamp>/` にコピー + keep 世代
  剪定、best-effort、now 注入で決定的）+ `tools/backup_data.py`（CLI、cron/タスクスケジューラ日次推奨）。
- `main.py` 起動時バックアップ（`config.backup.on_startup` 既定 true、best-effort）。`.gitignore` に `backups/`。
- `tests/test_backup.py` 4 件。**fsync は未実施**（残: §残課題）。

### B1 — セッション締め→精算の導線（P0: 会計の最終目的が到達不能だった）
- `gui/ledger_view.py` 精算パネルに「セッション終了（close）」ボタン。reopen 不可のため **2 クリック確認**
  （`_pending_close`、セッション切替で解除）。close 後に `commit_settlement` が到達可能に。
- staff API `POST /api/staff/sessions/{id}/close`（404 `not_found` / 409 `already_closed`、write 所有のみ）
  + `ViewerApiClient.close_session`。`docs/contracts/viewer-api.md` 更新。
- tests: GUI 3 件（2 クリック→close→commit / session 必須 / 切替で pending 解除）+ staff API 2 件。

## Changed Files

- code: `rfid/reader_thread.py`, `docs/installation.md`, `core/backup.py`(新), `tools/backup_data.py`(新),
  `main.py`, `config_default.json`, `.gitignore`, `gui/ledger_view.py`, `api/server.py`, `api/client.py`
- tests: `tests/test_rfid.py`, `tests/test_backup.py`(新), `tests/test_ledger_view_gui.py`,
  `tests/test_viewer_api_staff.py`
- docs: `docs/contracts/viewer-api.md`, `CHANGELOG.md`, 本 worklog, 並びに
  `docs/reviews/2026-06-15-v1.0-launch-review.md`（着手状況追記）

## Test Results

- 各コミットで `pytest tests/ --ignore=tests/test_vision.py -q` green: B3→624 / B2→628 / B1→**633 passed,
  0 skipped**。`ruff check .` clean。`config_default.json` JSON 妥当。

## 追記（2026-06-15）: B2 fsync 完了

全永続書き込みを共有ヘルパ `core/atomic_io.py:atomic_write_json`（temp→flush→fsync→os.replace）に集約。
9 writer（json_writer / player / session / ledger / order_request / player_credential / auth_identity /
sync / card_master）を移行し、各ファイルから重複 atomic-write を除去（`import os` も不要化）。fsync は
ファイル内容のみ（dir fsync は Windows 非対応のため不実施）。`tests/test_atomic_io.py` 4 件。637 passed。

## 追記（2026-06-15）: B7 完了（データ堅牢性）

- 破損退避: `core/atomic_io.read_json_file`（JSONDecodeError でファイルを `*.corrupt-<ts>` に退避 + ERROR、
  上書き消失を防ぐ）。players/sessions/ledger/order の `_load` を移行。`json` import を不要化。
- 可視化: ledger `_load` の不正レコード skip 件数を ERROR で集約。
- 並行安全: `PlayerRepository` / `SessionRepository` に RLock + `_locked`（ledger/order は既に lock 済）。
- tests: `tests/test_atomic_io.py` に quarantine 4 件追加（計 8）。641 passed。

## Remaining Gaps / 残課題（レビュー Blocker のうち未着手）

- **B9**: `adjustment` 負 cash の用途制限明文化 + 監査（アミューズ・ガードレール）。← 次
- **B8**: クリーン環境 + 実機の通し E2E（Phase H、来週の実機テストで実施）。manual-qa の stale 更新。
- **B4 訂正 UI**: iPad アプリ側で実装する方針（ユーザー決定）→ 別途 mobile/API の write path 設計。
- **B5/hosted 系**: SaaS フェーズ（後ろ倒し）。

## Related

- レビュー: `docs/reviews/2026-06-15-v1.0-launch-review.md`
- ADR-0015/0034（RFID）/ ADR-0016/0023（会計）/ ISSUE-0015
